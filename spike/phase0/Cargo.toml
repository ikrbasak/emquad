[package]
name = "phase0"
version = "0.1.0"
edition = "2021"

[dependencies]
typst = "=0.15.1"
typst-pdf = "=0.15.1"
typst-layout = "=0.15.1"
typst-assets = { version = "=0.15.1", features = ["fonts"] }
comemo = "0.5"

[profile.release]
lto = true
codegen-units = 1
strip = true
opt-level = 3
