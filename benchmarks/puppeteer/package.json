{
  "name": "emquad-puppeteer-benchmark",
  "version": "0.0.0",
  "private": true,
  "description": "emquad against Puppeteer on equivalent documents. Deliberately outside the pnpm workspace.",
  "type": "module",
  "scripts": {
    "bench": "node run.js"
  },
  "dependencies": {
    "puppeteer": "^24.0.0"
  }
}
