target/
node_modules/
*.node
dist/
.turbo/
.DS_Store

# Golden-file mismatch artifacts. Written on failure so a human can judge the
# change; never committed.
packages/*/test/golden/diff/

# `pnpm pack` output from the packaging tests.
*.tgz
